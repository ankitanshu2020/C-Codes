#include <stdio.h>
#include <string.h>

int main()
{
    char *st = "Ankitanshu";
    int a = strlen(st);
    printf("The string is %s\n", st);
    printf("Length of above string is %d", a);
    return 0;
}